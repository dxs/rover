<Page
    x:Class="rover.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rover"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20">
			<TextBlock Text="Pitch" FontSize="30" Margin="10"/>
			<TextBlock Text="Roll" FontSize="30" Margin="10"/>
			<TextBlock Text="Yaw" FontSize="30" Margin="10"/>
		</StackPanel>

		<StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20">
			<TextBlock x:Name="pitchBox" FontSize="30" Text="0.00" Margin="10"/>
			<TextBlock x:Name="rollBox" FontSize="30" Text="0.00" Margin="10"/>
			<TextBlock x:Name="yawBox" FontSize="30" Text="0.00" Margin="10"/>
		</StackPanel>
	</Grid>
</Page>
