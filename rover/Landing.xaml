<Page
    x:Class="rover.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rover"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBox x:Name="HelloMessage" Text="Choose text" Margin="10" IsReadOnly="False"/>
			<Button x:Name="Send" Content="Click Me!"  Margin="10" HorizontalAlignment="Center" Click="ClickMe_Click"/>
			<TextBlock x:Name="boxReception" Text="Text input" Margin="10" HorizontalAlignment="Center"/>
		</StackPanel>
	</Grid>
</Page>
