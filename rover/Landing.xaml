<Page
    x:Class="rover.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:rover"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<TextBlock Text="Live capture" HorizontalAlignment="Center" Grid.ColumnSpan="2" Margin="10"/>

		<StackPanel Grid.Column="0" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Center">
			<TextBox x:Name="xBox" IsReadOnly="True" Header="X Axis" Margin="10" Text="nothing yet"/>
			<TextBox x:Name="yBox" IsReadOnly="True" Header="Y Axis" Margin="10" Text="nothing yet"/>
			<TextBox x:Name="zBox" IsReadOnly="True" Header="Z Axis" Margin="10" Text="nothing yet"/>
		</StackPanel>

		<StackPanel Grid.Column="1" HorizontalAlignment="Center" Margin="10" VerticalAlignment="Center">
			<TextBox x:Name="xdBox" IsReadOnly="True" Header="Delta X" Margin="10" Text="nothing yet"/>
			<TextBox x:Name="ydBox" IsReadOnly="True" Header="Delta Y" Margin="10" Text="nothing yet"/>
			<TextBox x:Name="zdBox" IsReadOnly="True" Header="Delta Z" Margin="10" Text="nothing yet"/>
		</StackPanel>


	</Grid>
</Page>
